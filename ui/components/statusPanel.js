/*
    ========================================================
    STATUS PANEL
    ========================================================
*/


function updateStatus(
    title,
    description
) {

    console.log(
        `${title}: ${description}`
    );

}


window.updateStatus =
    updateStatus;